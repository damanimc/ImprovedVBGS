seed: 0
experiment_name: benchmark-imagenet 
model:
  n_components: 2000
  init_random: True
  dof: 1.0
  scale: null
  color_scale: null 
  position_scale: null
train:
  batch_size: 25120
  learning_rate: 1.0
  beta: 0.0
  n_iters: 1
output_root: ${oc.env:VBGS_OUTPUT_DIR,../output}
run_name: ${experiment_name}
hydra:
  job:
    chdir: True
  run:
    dir: ${output_root}/${run_name}
  sweep:
    dir: ${output_root}/sweep/${run_name}
    subdir: ""